# crypto-bot

discord bot
todo

-   allow user to pick 7d/24h/1h when setting embed colour in /coin
-   allow user to pick "favourite" cryptos and then let them quickly access them through a select menu from /coin
-   for alerting, try to batch notifs when updating cmc_cache, then push those batched notifs into a queue and stagger the alerts
-   for managealerts, use a multiselect menu to represent the individual notificatoin settings and then do "disable selected" "delete selected" "enable selected" (use diff colours for each button)