# crypto-bot

discord bot
todo

- allow user to pick 7d/24h/1h when setting embed colour in /coin
- make sure other users can't click other ppl's buttons
- for alerting, try to batch notifs when updating cmc_cache, then push those batched notifs into a queue and stagger the
  alerts
- for managealerts, use a multiselect menu to represent the individual notificatoin settings and then do "disable
  selected" "delete selected" "enable selected" (use diff colours for each button)